<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ls.mapper.primary.UserMapper">
    <select id="selectUserList" resultType="com.ls.domain.User">
        SELECT * FROM  user;
    </select>

</mapper>